{
  "name": "@stratum/worker",
  "version": "0.4.0",
  "description": "Stratum API — Cloudflare Worker + Durable Object log gate, serves the Atrium.",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@stratum/core": "*",
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260807.2",
    "wrangler": "^4.120.0"
  }
}
